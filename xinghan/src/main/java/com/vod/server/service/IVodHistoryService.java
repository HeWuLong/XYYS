package com.vod.server.service;
import com.baomidou.mybatisplus.extension.service.IService;
import com.vod.server.entity.domain.VodHistory;

public interface IVodHistoryService extends IService<VodHistory> {
    // 如果有复杂的自定义业务逻辑，在这里写接口定义
}